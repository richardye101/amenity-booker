# Copy to .env and fill in. Enables unattended login (headless server, no manual
# sign-in window). Keep this file private:  chmod 600 .env
BL_USERNAME=your-buildinglink-email
BL_PASSWORD=your-password

# Optional:
# PORT=3000
# HOST=127.0.0.1   # bind localhost only (reach via SSH tunnel)
